import { DataTableCellDirective } from './data-table-cell-directive';

describe('DataTableCellDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableCellDirective();
    expect(directive).toBeTruthy();
  });
});
